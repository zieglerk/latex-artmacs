\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{c-layout}[2009/06/15 v. 0.2 konstantin's personal collection
of LaTeX-packages (aka styles)]

%%% -------
%%% Policy
%%% ------

%%% - Im besten Fall herrscht Kompatibilitaet mit LaTeX und pdfLaTeX. Die Machbarkeit ist im Einzelfall (insbesondere fuer PSTricks) aber noch zu ueberpruefen.
%%% - sty-Dateien muessen im gleichen Verzeichnis wie die Datei liegen, die sie benutzen -- oder im lokalen texmf-Verzeichnis. In letztem Fall darf aber das Aktualisieren der Datenbank nicht vergessen werden.

%%% -------------------------------------------------
%%% Structure -> move to latex--c.org
%%% -------------------------------------------------

%%% 1. Pakete: RequirePackage (anstatt usepackage um doppeltes Laden zu verhindern) etc.
%%% 2. Anpassungen: setlength etc.
%%% 3. Makros: newcommand, DeclareMathOperator, etc.

%%% ----------------------
%%% Updating and Bugfixing
%%% ----------------------

\RequirePackage{verbatim} % reimplementation of the verbatim and verbatim* environments, along with a comment environment and the command \verbatiminput{<filename>}

%%% -------------------
%%% Language, Fonts and Layout
%%% -------------------

\RequirePackage[ngerman, english]{babel} % ??? makes english default, but
                                % enables to swith to german by
                                % \selectlanguage???
\usepackage[T1]{fontenc}

% \RequirePackage{txfonts} % ??? some sort of times roman?

% Typessetting URLs, email addresses and directories/paths
\RequirePackage{url}
\DeclareUrlCommand\email{\urlstyle{tt}}
\DeclareUrlCommand\directoryemail{\urlstyle{tt}}
% alternatively: \DeclareUrlCommand\email{\urlstyle{rm}}

%%% -----------------------
%%% develop, draft and edit
%%% -----------------------

\RequirePackage[ % textsize=tiny, % as soon as the todonotes package
                 % gets update on MikTeX
textwidth=2.5\marginparwidth]{todonotes} % allows todo-marks with \todo[]{} and a list of todos
                       % with \listoftodos. Switched off by global option
                       % `disable'.
% In case the todonotes breaks again, the following two lines neutralize its commands:
% \newcommand{\todo}[1]{}
% \newcommand{\listoftodos}{}
%%% \usepackage{todo} % alternative to `todonotes' which requires fewer other
%%% packages, but seems incompatible with the environments of the cc-class.
\RequirePackage{showkeys} % Show labels. Switch off by global option
                          % `final'.
\RequirePackage[scrtime]{prelim2e} % puts date and time under a draft
% use the scrtime package of the
                               % koma-script package to compute the
                               % time
% the option draft is default, the option final produces no output
\renewcommand{\PrelimWords}{Draft}

%%% ------------
%%% AMS environments
%%% ------------

\RequirePackage{amsmath, amsthm}

% theorems and equations share the same counter;
% to make it include the section number we specify:
\numberwithin{equation}{section}

\theoremstyle{plain}
\newtheorem{corollary}[equation]{Corollary}
\newtheorem{theorem}[equation]{Theorem}
\newtheorem{lemma}[equation]{Lemma}
\newtheorem{proposition}[equation]{Proposition}
\newtheorem{conjecture}[equation]{Conjecture}
\newtheorem{criterion}[equation]{Criterion}
\newtheorem{algorithm}[equation]{Algorithm}
\newtheorem{fact}[equation]{Fact}

\theoremstyle{definition}
\newtheorem{definition}[equation]{Definition}
\newtheorem{convention}[equation]{Convention}
\newtheorem{condition}[equation]{Condition}
\newtheorem{example}[equation]{Example}
\newtheorem{problem}[equation]{Problem}
\newtheorem{exercise}[equation]{Exercise}
\newtheorem{notation}[equation]{Notation}
\newtheorem{counterexample}[equation]{Counterexample}
\newtheorem{hypothesis}[equation]{Hypothesis}
\newtheorem{assumption}[equation]{Assumption}

\theoremstyle{remark}
\newtheorem{remark}[equation]{Remark}
\newtheorem{note}[equation]{Note}
\newtheorem{observation}[equation]{Observation}
\newtheorem{summary}[equation]{summary}
\newtheorem{claim}[equation]{Claim}
\newtheorem{acknowledgment}[equation]{Acknowledgment}
\newtheorem{case}[equation]{Case}
\newtheorem{conclusion}[equation]{Conclusion}
\newtheorem{result}[equation]{Result}
\newtheorem{answer}[equation]{Answer}
\newtheorem{question}[equation]{Question}
\newtheorem{openquestion}[equation]{Open Question}
\newtheorem{warning}[equation]{Warning}

%%% ------------
%%% commands
%%% ------------

% Deliminators
\newcommand{\abs}[1]{\left| #1 \right|}
\newcommand{\floor}[1]{\left\lfloor #1 \right\rfloor}

%%% ------------
%%% AMS Symbols
%%% ------------

\RequirePackage{amssymb, amsfonts}

% Sets
\DeclareMathOperator{\NN}{\mathbb{N}}
\DeclareMathOperator{\ZZ}{\mathbb{Z}}
\DeclareMathOperator{\QQ}{\mathbb{Q}}
\DeclareMathOperator{\RR}{\mathbb{R}}
\DeclareMathOperator{\FF}{\mathbb{F}}
% already defined \DeclareMathOperator{\PP}{\mathbb{P}} for the projective P
% but unfortunately \AA defined as some Angstrom A

% Operators
\DeclareMathOperator{\im}{im}
\DeclareMathOperator{\codim}{codim}

% Functors
\DeclareMathOperator{\Gal}{Gal}

%%% ------------
%%% colors and graphics
%%% ------------

\RequirePackage{xcolor} % change fontcolor within a group with \color{red}

%%% ------------
%%% environments
%%% ------------

\RequirePackage{listings} 

%%% !!!!!!!!!!!
%%% WORKINGMARK
%%% !!!!!!!!!!!

% %%% Drafting
% 
% \usepackage{showkeys} % Anzeigen der labels, Ausschalten durch option
%                       % final , am besten in den global options, dann
%                       % kann dieses Paket auch in unsere settings wandern
% 
% %%% Special Symbols
% 
% \usepackage{SIunits} % to avoid formatting units by hand, e.g.
% % \unit{32,1}{\micro\metre}
% 
% %%% Tables and Tabular
% 
% \usepackage{ctable} % for better spacing in the tabular environment
% 
% \usepackage{array} % redefines the tabular environment in such a way that it allows you to already declare mathematical mode when defining the table, e.g.
% % \begin{tabular}{>{\(} l <{\)} >{\(} l <{\)} >{\(} l <{\)}}
% % starts a table where all three columns contain material to be typeset in math mode.
% % When many tables are in use, it may be advisable to define new standard column types, e.g.
% % \newcolumntype{L}{>{\(} l <{\)}}
% % and hence defining the table above by
% % \begin{tabular{LLL}}
% 
% %%% Fine-Tuning
% 
% \usepackage{microtype} % highly recommend when using pdfLaTeX (plain LaTeX can not make use of it), because it improves line filling with:
% % - font expansion: it horizontally expands the characters in order to optimally fill each line;
% % - character protrusion: it lets some characters protrude into the margins (typically the hyphens and punctuation signs).
